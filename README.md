# 🏢 Telegram-бот бронирования аудиторий КЦ «Дар»

Telegram-бот для упрощения внутреннего бронирования помещений культурного центра «Дар». Бот предоставляет удобный интерфейс для просмотра информации об аудиториях, бронирования помещений и управления заявками.

## ✨ Функциональность

### 🔍 Просмотр аудиторий
- **По этажам**: 2, 3, 4 этажи
- **Детальная информация**: размер, мебель, оборудование, фото, комментарии
- **Оборудование**: монитор, флипчарт, кондиционер

### 📅 Бронирование
- Выбор аудитории по номеру
- Ввод ФИО и цели мероприятия
- Выбор даты и времени
- Проверка доступности
- Подтверждение бронирования

### 🔎 Управление бронированиями
- Просмотр активных бронирований пользователя
- Админ-панель для управления всеми заявками
- Статусы: pending, confirmed, rejected, cancelled

### 🛠 Админ-панель
- Доступ по паролю: `AdminDAR`
- Просмотр всех бронирований
- Управление статусами заявок
- Статистика по аудиториям

## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd telegram-booking-bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения
Скопируйте файл `.env.example` в `.env` и заполните необходимые данные:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Supabase Configuration
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_anon_key_here

# Admin Password (можно изменить)
ADMIN_PASSWORD=AdminDAR
```

### 4. Настройка базы данных Supabase

#### 4.1 Создание проекта в Supabase
1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Получите URL и API ключ из настроек проекта

#### 4.2 Выполнение SQL-скрипта
1. В Supabase Dashboard перейдите в SQL Editor
2. Скопируйте содержимое файла `database_setup.sql`
3. Выполните скрипт

#### 4.3 Настройка RLS (Row Level Security)
Скрипт автоматически создает политики безопасности. Убедитесь, что RLS включен для всех таблиц.

### 5. Запуск бота
```bash
python bot.py
```

## 🗄️ Структура базы данных

### Таблицы

#### `users`
- `id` - уникальный идентификатор
- `telegram_id` - ID пользователя в Telegram
- `username` - имя пользователя в Telegram
- `full_name` - полное имя
- `is_admin` - статус администратора
- `created_at`, `updated_at` - временные метки

#### `rooms`
- `id` - уникальный идентификатор
- `room_number` - номер аудитории
- `name` - название аудитории
- `floor` - этаж (2, 3, 4)
- `area` - площадь в кв.м
- `chairs`, `tables` - количество стульев и столов
- `monitor`, `flipchart`, `air_conditioning` - наличие оборудования
- `photo_url` - ссылка на фото
- `comments` - дополнительные комментарии
- `is_active` - активность аудитории

#### `bookings`
- `id` - уникальный идентификатор
- `user_id` - ссылка на пользователя
- `room_id` - ссылка на аудиторию
- `full_name` - ФИО забронировавшего
- `purpose` - цель мероприятия
- `start_time`, `end_time` - время начала и окончания
- `status` - статус бронирования
- `created_at`, `updated_at` - временные метки

### Представления и функции

#### `booking_details`
Объединяет информацию о бронированиях, аудиториях и пользователях.

#### `room_availability`
Показывает доступность аудиторий и количество активных бронирований.

#### `check_room_availability()`
Функция для проверки доступности аудитории в указанное время.

#### `get_room_schedule()`
Функция для получения расписания аудитории на определенную дату.

#### `get_room_statistics()`
Функция для получения статистики по аудиториям.

## 🔧 Архитектура проекта

```
telegram-booking-bot/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация и переменные окружения
├── database.py         # Менеджер базы данных
├── handlers.py         # Обработчики команд и сообщений
├── keyboards.py        # Клавиатуры и кнопки
├── requirements.txt    # Зависимости Python
├── database_setup.sql # SQL-скрипт для настройки БД
├── .env.example       # Пример файла конфигурации
└── README.md          # Документация
```

## 📱 Использование бота

### Главное меню
- **🗂 Аудитории** - просмотр информации об аудиториях
- **📅 Забронировать** - забронировать аудиторию
- **🔎 Мои брони** - просмотр активных бронирований
- **🛠 Админ-панель** - доступ к админ-функциям
- **ℹ️ Помощь** - справка по использованию

### Процесс бронирования
1. Выберите "📅 Забронировать"
2. Введите номер аудитории
3. Укажите ваше ФИО
4. Опишите цель мероприятия
5. Выберите дату (формат: ДД.ММ.ГГГГ)
6. Укажите время начала (формат: ЧЧ:ММ)
7. Укажите время окончания (формат: ЧЧ:ММ)
8. Подтвердите бронирование

### Админ-панель
- Пароль по умолчанию: `AdminDAR`
- Просмотр всех бронирований
- Управление статусами заявок
- Статистика по аудиториям

## 🔒 Безопасность

- **Row Level Security (RLS)** включен для всех таблиц
- Пользователи видят только свои бронирования
- Администраторы имеют доступ ко всем данным
- Пароль администратора настраивается в переменных окружения

## 🚨 Устранение неполадок

### Ошибка подключения к Supabase
- Проверьте правильность URL и API ключа в `.env`
- Убедитесь, что проект Supabase активен

### Ошибка Telegram Bot Token
- Проверьте правильность токена бота
- Убедитесь, что бот не заблокирован

### Проблемы с базой данных
- Проверьте выполнение SQL-скрипта
- Убедитесь, что RLS политики созданы корректно

## 📈 Расширение функциональности

### Возможные улучшения
- Интеграция с Google Calendar
- Уведомления о предстоящих бронированиях
- Экспорт данных в Excel/Google Sheets
- Мобильное приложение
- API для внешних систем

### Добавление новых аудиторий
```sql
INSERT INTO rooms (room_number, name, floor, area, chairs, tables, monitor, flipchart, air_conditioning, comments)
VALUES ('205', 'Новая аудитория', 2, 30, 15, 3, TRUE, TRUE, TRUE, 'Описание');
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь в корректности настроек
3. Проверьте статус Supabase проекта
4. Обратитесь к администратору системы

## 📄 Лицензия

Проект распространяется под лицензией MIT.

---

**Разработано для культурного центра «Дар»** 🎭