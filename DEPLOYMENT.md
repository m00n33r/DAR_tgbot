# 🚀 Инструкции по развертыванию

## 📋 Предварительные требования

### Системные требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)
- Git
- Доступ к интернету

### Аккаунты и сервисы
- Telegram аккаунт (для создания бота)
- Supabase аккаунт (для базы данных)

## 🔧 Пошаговая установка

### Шаг 1: Создание Telegram бота

1. **Откройте Telegram** и найдите @BotFather
2. **Отправьте команду** `/newbot`
3. **Введите имя бота** (например: "КЦ Дар - Бронирование")
4. **Введите username бота** (например: `dar_booking_bot`)
5. **Сохраните токен бота** - он понадобится позже

### Шаг 2: Создание проекта Supabase

1. **Перейдите на** [supabase.com](https://supabase.com)
2. **Нажмите "Start your project"**
3. **Войдите или зарегистрируйтесь**
4. **Создайте новый проект:**
   - Название: `dar-booking-bot`
   - База данных: `postgres`
   - Регион: выберите ближайший к вам
5. **Дождитесь создания проекта** (может занять несколько минут)

### Шаг 3: Получение данных Supabase

1. **В Dashboard проекта** перейдите в Settings → API
2. **Скопируйте:**
   - Project URL
   - anon public key

### Шаг 4: Клонирование и настройка проекта

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd telegram-booking-bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Для Windows:
venv\Scripts\activate
# Для Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 5: Настройка переменных окружения

```bash
# Скопируйте файл конфигурации
cp .env.example .env

# Отредактируйте .env файл
nano .env  # или используйте любой текстовый редактор
```

**Содержимое .env файла:**
```env
# Telegram Bot Token (полученный от @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Supabase Configuration (из Dashboard проекта)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Admin Password (можно изменить)
ADMIN_PASSWORD=AdminDAR
```

### Шаг 6: Настройка базы данных

1. **В Supabase Dashboard** перейдите в SQL Editor
2. **Создайте новый запрос**
3. **Скопируйте содержимое** файла `database_setup.sql`
4. **Выполните скрипт** (нажмите "Run")

### Шаг 7: Проверка настройки базы данных

1. **Перейдите в Table Editor**
2. **Убедитесь, что созданы таблицы:**
   - `users`
   - `rooms`
   - `bookings`
3. **Проверьте, что в таблице `rooms` есть тестовые данные**

### Шаг 8: Запуск бота

```bash
# Убедитесь, что виртуальное окружение активировано
python bot.py
```

**Ожидаемый вывод:**
```
2024-01-01 12:00:00,000 - __main__ - INFO - Бот запускается...
```

## 🧪 Тестирование

### Тест 1: Проверка подключения к боту
1. **Найдите вашего бота** в Telegram по username
2. **Отправьте команду** `/start`
3. **Убедитесь, что появилось главное меню**

### Тест 2: Просмотр аудиторий
1. **Нажмите "🗂 Аудитории"**
2. **Выберите этаж** (например, "2 этаж")
3. **Проверьте список аудиторий**

### Тест 3: Тестовое бронирование
1. **Нажмите "📅 Забронировать"**
2. **Введите номер аудитории** (например, "201")
3. **Заполните все поля** и подтвердите
4. **Проверьте создание записи** в базе данных

### Тест 4: Админ-панель
1. **Нажмите "🛠 Админ-панель"**
2. **Введите пароль** `AdminDAR`
3. **Проверьте доступ** к админ-функциям

## 🚨 Устранение неполадок

### Ошибка: "TELEGRAM_BOT_TOKEN не установлен"
**Решение:** Проверьте правильность токена в `.env` файле

### Ошибка: "SUPABASE_URL не установлен"
**Решение:** Проверьте URL проекта в `.env` файле

### Ошибка: "Ошибка при создании бронирования"
**Решение:** 
1. Проверьте подключение к Supabase
2. Убедитесь, что таблицы созданы корректно
3. Проверьте логи бота

### Ошибка: "Доступ запрещен" в админ-панели
**Решение:**
1. Проверьте правильность пароля
2. Убедитесь, что пользователь создан в базе данных
3. Проверьте поле `is_admin` в таблице `users`

## 🔄 Обновление бота

```bash
# Остановите бота (Ctrl+C)
# Обновите код
git pull origin main

# Перезапустите
python bot.py
```

## 📊 Мониторинг

### Логи бота
Бот автоматически ведет логи в консоли. Для продакшена рекомендуется настроить логирование в файл:

```python
# В bot.py добавьте:
import logging
from logging.handlers import RotatingFileHandler

# Настройка логирования в файл
file_handler = RotatingFileHandler('bot.log', maxBytes=1024*1024, backupCount=5)
file_handler.setFormatter(logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s'))
logging.getLogger().addHandler(file_handler)
```

### Мониторинг базы данных
В Supabase Dashboard доступны:
- Статистика запросов
- Мониторинг производительности
- Логи ошибок

## 🔒 Безопасность

### Рекомендации по безопасности
1. **Измените пароль администратора** по умолчанию
2. **Ограничьте доступ** к Supabase Dashboard
3. **Регулярно обновляйте** зависимости
4. **Мониторьте логи** на подозрительную активность

### Настройка RLS
Row Level Security уже настроен в SQL-скрипте. Убедитесь, что:
- RLS включен для всех таблиц
- Политики безопасности созданы корректно
- Тестовые пользователи не имеют избыточных прав

## 🚀 Продакшн развертывание

### Рекомендации для продакшна
1. **Используйте процесс-менеджер** (systemd, supervisor)
2. **Настройте автозапуск** при перезагрузке сервера
3. **Настройте мониторинг** и алерты
4. **Регулярно создавайте резервные копии** базы данных

### Пример systemd сервиса
```ini
# /etc/systemd/system/dar-booking-bot.service
[Unit]
Description=DAR Booking Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/telegram-booking-bot
Environment=PATH=/path/to/telegram-booking-bot/venv/bin
ExecStart=/path/to/telegram-booking-bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

## 📞 Поддержка

При возникновении проблем:
1. **Проверьте логи** бота и Supabase
2. **Убедитесь в корректности** всех настроек
3. **Проверьте статус** внешних сервисов
4. **Обратитесь к документации** или создайте issue

---

**Успешного развертывания!** 🎉